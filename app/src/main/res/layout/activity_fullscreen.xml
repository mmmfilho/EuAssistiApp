<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:fresco="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:id="@+id/fullscreenLayout"
    >

    <TextView android:id="@+id/fullscreen_texto" android:layout_width="match_parent"
        android:layout_height="match_parent" android:keepScreenOn="true" android:textColor="@color/colorPrimary"
        android:textStyle="bold" android:textSize="50sp" android:gravity="center"
        android:text="EU ASSISTI" />

    <com.facebook.drawee.view.SimpleDraweeView
        android:id="@+id/sdview_intro"
        android:layout_width="200dp"
        android:layout_height="200dp"
        android:layout_gravity="center_horizontal"
        android:transitionName="element1"
        app:layout_collapseMode="parallax"
        fresco:actualImageScaleType="centerCrop"
        fresco:failureImage="@drawable/error_img"
        fresco:placeholderImage="@drawable/loading_img"
        fresco:retryImage="@drawable/retry_img" />


</FrameLayout>
